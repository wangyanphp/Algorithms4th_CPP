这一章主要是实现各种查找的数据结构
这里有一个问题需要解决：如果查不到怎么办？
在Java中，如果查不到返回null——这基于一个假设，所有的类型都可以识别null。在Java中一切解释对象的构架下，这一点很很容易实现。
但是，在C++中，如何实现这一点？
一种方法是————每一种类型都存在一个’null‘值。这需要对POD类型做一些特化处理。同时每种自定义类型都需要自定义自己的’null‘值。除非是自己定义一个框架，否则有点高射炮打蚊子的感觉。
另一种方法是————在这个算法里定义一个’null‘值。问题是如何出C++中大家都认同的'null'值？？？还是得借助于迭代器类型。


ST2017.h------------实现了二叉查找树和红黑树，以及哈希表。（但是没有解决null值问题）
3-1.cpp,3-2.cpp,3-5.cpp---------实现习题，本章中没有什么重要的习题，基本都是围绕着二叉查找树和红黑树这两个数据结构展开的。

st.h----------------根据书本中的算法，加了一层外包装：迭代器。
为此Node数据结构中添加了parent。具体可以参考《STL源码剖析》

二叉查找树和红黑树，有一种继承关系。C++一贯做法是使用模板来实现编译时多态，现在用起来不是那么得心应手。


